{{{imports tii "go"}}}{{{declarations tii "go"}}}
{{#each tii.transactions}}
func {{identifier @key "go" "method"}}(args {{paramsTypeName @key "go"}}) map[string]interface{} {
	return map[string]interface{}{
{{#each params.properties}}
		"{{@key}}": args.{{identifier @key "go" "param"}},
{{/each}}
	}
}

{{/each}}
