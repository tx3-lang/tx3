{{{imports tii "python"}}}{{{declarations tii "python"}}}
{{#each tii.transactions}}
def {{identifier @key "python" "method"}}(args: {{paramsTypeName @key "python"}}) -> dict:
    return {
{{#each params.properties}}
        "{{@key}}": args.{{identifier @key "python" "param"}},
{{/each}}
    }


{{/each}}
