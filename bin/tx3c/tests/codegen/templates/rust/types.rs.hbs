{{{imports tii "rust"}}}{{{declarations tii "rust"}}}
{{#each tii.transactions}}
impl From<{{paramsTypeName @key "rust"}}> for ArgMap {
    fn from(args: {{paramsTypeName @key "rust"}}) -> Self {
        let mut map = ArgMap::new();
{{#each params.properties}}
        map.insert("{{@key}}".to_string(), serde_json::to_value(&args.{{identifier @key "rust" "param"}}).unwrap());
{{/each}}
        map
    }
}

pub fn {{identifier @key "rust" "method"}}(args: {{paramsTypeName @key "rust"}}) {}
pub const {{identifier @key "rust" "constant"}}_TIR: &str = "";

{{/each}}
